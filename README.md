# Mi racha

Contador minimalista de hábitos con calendario, fechas de inicio y fin, y persistencia local.

## Uso

Abre `index.html` en el navegador o inicia un servidor local:

```bash
python3 -m http.server 4173
```

Los datos se guardan únicamente en el navegador mediante `localStorage`.
